#pragma once

namespace Sapphire
{
    namespace Nucleus
    {
        const int NUM_PARTICLES = 5;        // my Nucleus uses 5 particles, but yours doesn't have to! See NucleusEngine constructor.
        const float OUTPUT_EXPONENT = 10;
        const float INPUT_EXPONENT = 4;
        const float INPUT_SCALE = 0.015f;
    }
}
